[project]
name = "imsdt-vec-demo"
version = "0.1.0"
description = "Intent-driven digital twin demo for vehicular edge computing."
requires-python = ">=3.12"
dependencies = []

[tool.uv]
package = false
